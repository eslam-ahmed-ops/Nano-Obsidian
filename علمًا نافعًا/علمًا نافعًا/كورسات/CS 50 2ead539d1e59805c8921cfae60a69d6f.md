# CS 50

Category: Programming
Status: In progress
Progress: 62.5
Platform: Youtube
Age: 21
Rating: ⭐⭐⭐⭐⭐

# مقدمة

## Chapter

### Week 1: C Langunge

- **terminal codes**
    
    code name file.c = creat file
    
    make name file = compile > gcc name.c -o name
    
    ./name = run
    
- **C**
    
    لكي استدعي المكتبات في لغة C
    
    ```c
    #include <name.h>
    ```
    
    وname عباره عن اسم المكتبة
    
    اي امر برمجي يجب ان ينتهي ب (;)
    
    لكي اجعل عند انتهاء السطر يبدأ سطر جديد \n
    
    لكتابة تعليقات // 
    
    ```c
    // hello
    ```
    
    قبل اي متغير يجب تحديد نوعه في هذه اللغة لكي تفهم ما المطلوب منها مثل 
    
    string answer
    
    وهذه يعني ان المتغير answer  عباره عن string
    
    ```c
    printf ("hello, answer");
    
    ```
    
    هنا خطأ لانه سيطبع hello, answer ونحن لا نريد هذا فنكتب
    
    ```c
    printf ("hello, %s\n", answer);
    ```
    
    وهنا %s تقوم بحجز مكان للمتغير عند الطباعة ونكتب المتغير كما مكتوب اعلاه وسبب استخدام حرف s لانه string
    
    ماذا لو اريد حرف لوحده في متغير يبقي char وهو اختصار charcter
    
    وهناك انواع كثيرة موجودة في المصادر
    
    a=b 
    
    معنها ان قيمه a هي b ولكن لو  عايز اقارن يبقي a==b 
    
    لادخال اكثر من قيمة في if  استخدم (||) وهي معنها or 
    
    ولو عايز and (&&)هكذا
    
    ```c
    if (c == 'y' || c == 'Y')
    if (c == 'y' && c == 'Y')
    ```
    
    لو المتغير حرف واحد او هناك حرف واحد في القيمة استخدم(’) وليس (“)
    
    Loop
    
    ```c
    int counter = 3;
    		while (counter > 0)
    		{
    		printf("meow\n");
    		}
    		
    ```
    
    هيطبع الى مالا نهاية لكي اجعله يطبع الرقم المحدد 
    
    ```c
    int counter = 3;
    		while (counter > 0)
    		{
    		printf("meow\n");
    		counter = counter -1;
    		}
    		
    		or
    counter--;
    		or
    counter -= 1;
    لو اكبر 
    counter++;
    ```
    
    For
    
    ```c
    for (int i=0; i<10; i++)
    ```
    
    لو عايز اعمل fountions للتعريف استخدم void name (n)
    
    حيث n القيمة التي اريدها لو لا يوجد اكتب void
    
    ```c
    void meow(void)
    {
    printf ("meow\n")
    }
    
     للاستدعاء
     meow();
     
     like this
     void meow(void)
    {
        printf("Meow!\n");
    }
    int main (void)
    {
        for (int i = 0; i < 3; i++)
        {
            meow();
        }
    }
    ```
    
    وهذا غير منطقي لان int main هي اساس البرنامج ولا يجوز ان يأتي قبلها شيء فنكتب هكذا
    
    ```c
    void meow(void);
    int main (void)
    {
        for (int i = 0; i < 3; i++)
        {
            meow();
        }
    }
    void meow(void)
    {
        printf("meow\n");
    }
    ومعناه اني سوف استدعي ولكن بعدين
    ```
    
    لو عايزها تحمل قيمة مثلا ولازم اعرف نوع البيانات void meow (int n) 
    
    ```c
    void meow(int n);
    int main (void)
    {
        int n = get_int("Number of meows: ");
        meow(n);
    }
    void meow(int n)
    {
        for (int i = 0; i < n; i++)
        {
            printf("meow\n");
        }
    }
    ```
    
    لو  انا عايز اعمل function وترجع قيمة استبدل void الاولى بنوع القيمة المراد استرجاعها وفي void الثانية نطاق الfuntions مثل
    
    ```c
    int add(int a, int b)
    {
        return a + b;
    }
    ```
    
    لكي امنع تجاهل العمليه لو اعطيت رقم int وليس float اكتب كالتالى  
    
    ```c
    (float)x // سيحول المتغير الى float
    ```
    
    واسم العملية التجواز او truncation 
    
    ولتحديد كام رقم بعد العلامة 
    
    ```c
    %0.3f
    ```
    
    الشبيه ب float ولكن تخزن 64 بت نستخدم double ويقبل نفس البرسنت %f
    
    ل
    
    ل
    
- **Linux command**
    
    ```c
    to rename file -> mv name1.c ->name2.c
    to del file -> rm name.c
    to create folder -> mkdir name
    to del folder -> rmdir name
    to show list -> ls
    to clear -> ctrl + l
    cd -> open folder
    cp
    to create or open file-> code name.c
    ```
    
- **mario.c**
    
    لو اريد متغير يدخل مره واحده ولا تتغير قيمته قبل المتغير اكتب const
    
    ```c
    const int n = 4;
    ```
    
    لاجبار الشخص على ادخال مدخل معين استخدم while
    
    ```c
        const int n = get_int("Height: ");
        while (n < 1)
        {
            n = get_int("Height: ");
        }
    ```
    
    لكن هذا غير منطقي لاني استخدمت نفس الجملة مرتين وايضا  بها ثابت فالحل اكتبها باستخدام دالة  do while 
    
    ```c
    int main (void)
    {
        int n; // تم حجز المتغير 
        do
        {
            n = get_int("Height: ");// استخدم هنا
        }
        while (n < 1);// وهنا
        for (int i = 0; i < n; i++)
        {
            for (int k = 0; k < n; k++)
            {
                printf("#");
            }
            printf("\n");
        }
    
    }
    ```
    
    kl
    
- integer overflow
    
    وهو عندما لا تكون البتات كافية يقوم العداد باعادة الدوران مثلا تمثيل الرقم 8 محتاج 4 بتات ولكن هناك 3 فقل فعند اللف مره اخري يعطي 3 بتات قيمة 000 وهو تمثيل للصفر والواحد في البيت الرابع غير موجود 
    
    في النوع int يقبل 32 بت من البيانات 
    
    وهناك نوع اخر هو long وهو يتعامل مع 64 بت
    
- Data type
    
    bool
    
    char
    
    double
    
    float
    
    int
    
    long
    
    string
    

### Week 2: Arrays

- **debugging**

### Week 3: Algorithms

### Week 4: Memory

### Week 5: Data Structure

### Week 6: Python

### Week 7: Artificial Intelligence

### Week 8: SQL

## **source**

CS50 Library→ manual.cs50.io

## Links